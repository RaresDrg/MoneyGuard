import styled from "styled-components";
import TextLoader from "./TextLoader";

const StyledTextLoader = styled(TextLoader)`
  width: fit-content;
  font-weight: 400;
  font-size: 16px;
  line-height: 24px;
  font-style: italic;
  color: #ffffff99;
`;

export default StyledTextLoader;

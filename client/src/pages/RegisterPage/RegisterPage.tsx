import { Section } from "../../components/common";
import { RegisterForm } from "../../components";

const RegisterPage = () => {
  return (
    <Section variant="registerBg">
      <RegisterForm />
    </Section>
  );
};

export default RegisterPage;
